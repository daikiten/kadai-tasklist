<h1>タスク一覧</h1>


  
  <% @tasks.each do | task |%>
  <ul>
      <li><%= link_to task.id, task %>
       　&gt;
      <%= task.status %>
       　
     <%= task.content %></li>
    </ul>
    
    <% end %>


<p><%= link_to '新規メッセージの投稿', new_task_path %></p>