タスク一覧


  
  <% @tasks.each do | task |%>
  
    
      <%= link_to task.id, task %>
      
       <%= task.id %>
       　&gt;
      <%= task.status %>
       　
      <%= task.content %>
    
    
    <% end %>


<%= link_to '新規メッセージの投稿', new_task_path %>